defmodule MovingAverage do
  @doc """
  Checks indicators related to moving averages
  """

  def simple(moving_average, current_tick, previous_tick) do

  end

  def exponential(moving_average, current_tick, previous_tick) do

  end
end
